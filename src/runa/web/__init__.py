"""web/: the `runa ui` dashboard -- Agents, Sessions, Traces, Evaluations, read-only.

`app.py` builds the FastAPI app; `agents.py`/`sessions.py`/`traces.py`/`evaluations.py` each
render one section's pages. See `cli/ui.py` for the `runa ui` command that serves it.
"""
